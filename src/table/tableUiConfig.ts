/** Per-player turn label banners ("Your turn" / "Bob's turn") — redundant with timer. */
export const ENABLE_TURN_LABEL_BANNERS = false;

/** Hero-only late "Your Turn" graphic after idle wait (~15s, matches turn countdown). */
export const ENABLE_HERO_TURN_REMINDER = true;
