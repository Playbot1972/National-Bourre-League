# Phase 3 public-table soak / staging — server rollout guard (not a secret).
# Loaded by firebase-tools on deploy for project national-bourre-league.
# Client flag MIXED_PUBLIC_TABLES_CLIENT_ENABLED remains false in code.
MIXED_PUBLIC_TABLES_SERVER_ENABLED=true
